export type TNavLink = {
  name: string,
  link: string
}

export type TProject = {
  name: string,
  stack: string[],
  description: string,
  link: string,
  image: string,
  gif: string,
}

export type TSkillList = {
  section: string,
  skills: string[]
}